const variant__button = document.getElementById('variant__button');
const variant__input = document.getElementById('variant__input');

variant__button.addEventListener('click', async () => {
    variant__input.value = '';
});